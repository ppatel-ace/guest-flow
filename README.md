# GuestFlow

Customer invitation and check-in management application with email invitations (QR codes), dual check-in methods (QR scan/email-phone lookup), flexible data entry (manual, online forms, CSV/Excel import), Excel export, analytics dashboard, customer management, and public pages (/scan and /guest-check-in).
